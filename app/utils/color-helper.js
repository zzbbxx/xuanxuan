import {
    convertColorToString,
    convertHexToRGB,
    decomposeColor,
    getContrastRatio,
    getLuminance,
    emphasize,
    fade,
    darken,
    lighten
} from 'material-ui/utils/colorManipulator';

const helper = {
    convertColorToString,
    convertHexToRGB,
    decomposeColor,
    getContrastRatio,
    getLuminance,
    emphasize,
    fade,
    darken,
    lighten
};

export default helper;
